<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk android:minSdkVersion="21" />

    <application
        android:allowBackup="false"
        android:hasCode="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="Fleet ServiceConnection Fix">

        <!-- LSPosed / Xposed module markers -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposeddescription"
            android:value="Fixes the onServiceConnected(...IBinderSession) NoSuchMethodError startup crash for com.Leiting.Fleet on Android 16 (API 36.1)." />
        <!-- Auto-suggested scope in LSPosed Manager -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
    </application>

</manifest>
